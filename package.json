{
  "name": "tree-sitter-postgresql",
  "version": "1.0.0",
  "description": "",
  "main": "bindings/node",
  "scripts": {
    "build": "node-gyp build",
    "generate": "tree-sitter generate",
    "playground": "tree-sitter generate && sudo tree-sitter build-wasm && tree-sitter playground",
    "test": "npm run generate && tree-sitter test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.14.2"
  },
  "devDependencies": {
    "@types/node": "^16.4.7",
    "tree-sitter-cli": "^0.17.3",
    "typescript": "^4.3.5"
  }
}
